/**
 * Delete a car from the database
 * - if there is no such car, redirect to /cars
 * - if there is a car, delete it and redirect to /cars
 */

var requireOption = require('../common').requireOption;
